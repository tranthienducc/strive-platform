export enum FORM {
  CREATE = "create",
  UPDATE = "update",
}

export enum ACTION {
  WATCH = "watch",
}

export enum FILTERS_CATEGORIES {
  POPULAR = "Popular",
  RECENT = "Recent",
  PAID_FREE = "Paid + Free",
  PAID = "Paid",
  FREE = "Free",
}

export enum EOrderStatus {
  PENDING = "PENDING",
  PAID = "PAID",
  REJECTED = "REJECTED",
}
