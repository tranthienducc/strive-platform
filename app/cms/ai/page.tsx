// import Chat from "@/app/cms/ai/_component/Chat";

// const initialMessages = ["this is initial message chat bot ai"];

export default async function Ai() {
  return (
    // <Chat
    //   message={initialMessages.map((m: any, i: any) => ({
    //     ...m,
    //     id: i.toString(),
    //   }))}
    // />
    <div></div>
  );
}
